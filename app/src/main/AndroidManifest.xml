<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hoiwanlouis.myboggle" >

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />


    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".WelcomeActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <activity
                android:name="com.hoiwanlouis.myboggle.BoardActivity"
                android:label="@string/board_activity"
                android:parentActivityName="com.hoiwanlouis.myboggle.WelcomeActivity">
                <meta-data
                    android:name="android.support.PARENT_ACTIVITY"
                    android:value="com.hoiwanlouis.myboggle.WelcomeActivity"
                    />
            </activity>

            <activity
                android:name="com.hoiwanlouis.myboggle.SettingsActivity"
                android:label="@string/settings_activity" >
            </activity>

        </activity>
    </application>

</manifest>
